@PACKAGE_INIT@

if(NOT TARGET everest::helpers)
    add_library(everest::helpers INTERFACE IMPORTED)
    set_target_properties(everest::helpers PROPERTIES
        INTERFACE_INCLUDE_DIRECTORIES "${PACKAGE_PREFIX_DIR}/@CMAKE_INSTALL_INCLUDEDIR@"
    )
endif()

check_required_components(everest-helpers)
